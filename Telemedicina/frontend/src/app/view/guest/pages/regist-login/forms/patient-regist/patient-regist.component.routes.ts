import { Routes } from '@angular/router';
import { PatientRegistComponent } from './patient-regist.component';

export const routes: Routes = [
  {
    path: '',
    component: PatientRegistComponent,
  }
];
